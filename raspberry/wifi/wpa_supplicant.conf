# The content below should be put directly in a wpa_supplicant.conf file in the SD root directory after Flashing.
# Update value as needed
country=FR
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1
network={
  ssid="NOM_RESEAU"
  scan_ssid=1
  psk="MOTDEPASSE"
  key_mgmt=WPA-PSK
}
